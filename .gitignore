# Entorno
.env
.venv/
venv/
__pycache__/
*.pyc

# Base de datos local del prototipo
data/*.db
data/*.db-journal

# Datos clínicos reales (información sensible / habeas data - NUNCA a git)
data/*.xlsx
data/_processed/
Preguntas y respuestas medico.xlsx

# Excepción: no es dato clínico de paciente, es contenido de negocio
# (mapeo cluster -> programas del portafolio) que la app necesita para
# funcionar (ver app/domain/portfolio.py).
!data/recomendacion_clusters_portafolio.xlsx

# Streamlit
.streamlit/secrets.toml

# SO
.DS_Store
Thumbs.db

# Claude Code (config local, no compartida)
.claude/settings.local.json
