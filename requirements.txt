# Fenotipo Médico – Segmentación Inteligente de Pacientes (Comfama)
# Prototipo local

streamlit==1.40.0
sqlalchemy==2.0.32
pandas==2.2.2
plotly==5.23.0
python-dotenv==1.0.1
pyyaml==6.0.2
anthropic>=0.90.0
openpyxl==3.1.5

# Clasificador de fenotipo entrenado (app/domain/ml_model.py)
scikit-learn>=1.5
scipy>=1.13

# Consulta de exámenes médicos en Microsoft Fabric (consola médica).
# Requiere el "ODBC Driver 18 for SQL Server" instalado a nivel de sistema
# operativo (no se instala vía pip).
pyodbc>=5.1

# Texto a voz (ElevenLabs) para accesibilidad — botón "Escuchar" en el
# cuestionario.
requests>=2.31

# Voz a texto (Whisper local, sin API/costo) — botón de micrófono en el
# cuestionario. Requiere streamlit>=1.38 (st.audio_input).
faster-whisper>=1.0

# Solo para reentrenar el modelo / correr los notebooks de databricks/
# (no son dependencia de la app en producción)
# matplotlib>=3.8
# jupyter, nbformat, nbconvert
